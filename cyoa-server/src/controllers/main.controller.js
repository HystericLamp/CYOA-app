exports.intro = (request, response) => {
    response.json({ 
        message: 'Ok',
        status: 'active'
    });
};

// TODO: Implement a GET request to generate an intro scenario
// Provide a starter scenario to the user
// Preferably, not generated by AI, but randomly from a list of 10 scenarios
exports.generateStory = (request, response) => {
    const story = generateStory();
    response.json({
        story: story
    });
};

// TODO: Implement a POST request to generate a next story prompt
// Take user action and generate the next story scenario
exports.nextStoryPrompt = (request, response) => {
    response.json({
        message: 'Next story segment',
        currentPage: 1,
        choices: [
            { id: 1, text: 'Go left' },
            { id: 2, text: 'Go right' }
        ]
    });
};